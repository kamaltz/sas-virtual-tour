# Public URL of the published 3DVista Virtual Tour (set the real value in .env
# locally and in the Vercel project env vars — never commit the real URL here).
# If left empty, the Virtual Tour page shows a graceful placeholder (no error).
VITE_3DVISTA_TOUR_URL=
